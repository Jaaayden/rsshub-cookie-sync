#!/bin/sh
set -eu

# Keep this tiny launcher free of secrets.  The Python module owns all input
# validation and never receives cookies as command-line arguments.
exec /usr/bin/python3 /usr/local/lib/rsshub-cookie-sync/rsshub_cookie_sync.py \
    "$@" --config /etc/rsshub-cookie-sync/config.json
